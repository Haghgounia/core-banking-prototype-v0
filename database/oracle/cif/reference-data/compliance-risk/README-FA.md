# CIF Reference Data - Compliance, Risk and KYC (Phase 2)

این فاز 21 جدول مرجع حوزه تطبیق، ریسک و KYC را فعال می‌کند و 87 رکورد Seed دارد.

ترتیب اجرا در SQL Developer با Run Script / F5:

```sql
@install.sql
```

نکته: کد `NOT_VERIFIED` مستند مرجع در این نسخه به `UNVERIFIED` نرمال شده است تا با Default و کاربرد عملیاتی جداول CIF مانند PARTY و PARTY_IDENTIFIER همسان باشد.

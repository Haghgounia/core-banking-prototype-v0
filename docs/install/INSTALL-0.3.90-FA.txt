Core Banking Prototype 0.3.90 / FIX98
======================================

موضوع Release:
CBI Rial Banking Fee 1405 - Provisional Versioned Import + Temporal Archive

پیش‌نیاز:
- نصب موفق FEE Baseline 1.0.
- اجرای موفق تعرفه CBI-1404 (FIX81).
- نامه رسمی مصوبه جدید هنوز در اختیار پروژه نیست؛ Source جدید عمداً PROVISIONAL است.

مراحل ارتقا از 0.3.89:
1) bin\stop.cmd
2) Patch نسخه 0.3.90 را روی Root پروژه Overlay کنید.
3) در SQL*Plus/SQLcl از مسیر database\oracle\fee اجرا کنید:
   @install-cbi-rial-fee-1405-provisional.sql
4) خروجی Verify باید OK باشد و سپس COMMIT خودکار Installer انجام می‌شود.
5) build-production.cmd
6) پیام Built version: 0.3.90 را کنترل کنید.
7) bin\start.cmd
8) Ctrl+F5

اثر دیتابیس:
- DDL جدید ندارد.
- 152 تعرفه جدید + 180 Component + 15 Tier وارد می‌شود.
- 156 Version قبلی غیرالکترونیکی CBI-1404 به صورت Temporal Archive بسته می‌شود.
- 73 Version الکترونیکی CBI-1404 دست‌نخورده باقی می‌ماند.
- هیچ DELETE فیزیکی انجام نمی‌شود.

هشدار:
EFFECTIVE_FROM=2026-09-10 و CIRCULAR_NO=PROVISIONAL-RIAL-FEE-1405 مقادیر موقت Prototype هستند و بعد از دریافت نامه رسمی باید اصلاح شوند.

QA:
docs\FEE-0.3.90-FIX98-CBI-RIAL-1405-PROVISIONAL-QA.md

Core Banking Prototype 0.7.0 - نصب Phase 7 End-to-End Integration & Runtime Hardening
Baseline مورد انتظار: 0.6.0

1) Patch 0.7.0 را روی Source فعلی 0.6.0 Overlay کنید.
2) با کاربری که مجوز DDL/DML لازم روی DPS2 دارد اجرا کنید:
   @database/oracle/dps2/migrations/0.7.0-phase7-opening-e2e-hardening.sql

   این Migration قبل از ساخت Unique Guardها وجود Duplicateهای قدیمی را کنترل می‌کند.
   در صورت مشاهده -20701 / -20702 / -20703 ابتدا داده تکراری را بررسی و اصلاح کنید.

3) سپس در Windows:
   cd /d D:\Projects\core-banking-prototype-v0
   bin\stop.cmd
   rmdir /s /q frontend\dist
   rmdir /s /q backend\src\main\resources\static
   build-production.cmd
   bin\start.cmd

4) کنترل Build:
   - DPS2 Deposit Opening Phase 7 verification OK
   - Built version: 0.7.0
   - Backend tests: 0 Failures / 0 Errors

5) کنترل Runtime:
   http://localhost:8091/#/four-deposits/runtime-readiness
   - Readiness باید حداقل READY_WITH_WARNINGS باشد.
   - Rollback Probe باید PASS باشد.
   - ACCOUNT_SERVICE و TAX_PROFILE_SERVICE تا اتصال واقعی ممکن است WARN باقی بمانند.

6) کنترل Wizard:
   http://localhost:8091/#/four-deposits/opening
   در مرحله 5، «اجرای کنترل Runtime» باید Oracle/CIF/PDL را کنترل کند.

این Migration جدول Business جدید ایجاد نمی‌کند و فقط DB-level Idempotency/Concurrency Guard اضافه می‌کند.

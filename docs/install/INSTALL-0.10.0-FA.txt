Core Banking Prototype 0.10.0
Deposit Opening Operational v5 — Phase 10A/10B/10D

پيش‌نياز:
- Source و Database حداقل در Baseline 0.9.3 باشند.
- Reference Data / FK Reconciliation نسخه 0.9.3 قبلاً با SUCCESS اجرا شده باشد.

نصب کامل از 0.9.3:

1) Source نسخه 0.10.0 را Overlay/جايگزين کنيد.

2) Foundation مدل Operational v5 را با F5 اجرا کنيد:
   @database/oracle/dps2/migrations/0.10.0-phase10-opening-operational-v5-foundation.sql

   انتظار:
   SUCCESS: Phase 10 Operational Opening v5 schema foundation reconciled.

   Migration نسخه R2 semantics-aware است و FK معادل را با نام متفاوت دوباره ايجاد نمي‌کند.

3) اگر Foundation اوليه شما در بخش Constraint با ORA-02275 اجرا شده، Repair را اجرا کنيد:
   @database/oracle/dps2/migrations/0.10.0-phase10a-constraint-reconciliation-repair.sql

4) Runtime Alignment را اجرا کنيد:
   @database/oracle/dps2/migrations/0.10.0-phase10b-opening-operational-runtime-alignment.sql

   انتظار:
   SUCCESS: Phase 10B runtime schema alignment completed.

   ATTEMPT_AT در Funding Plan بايد تا اولين Settlement واقعي NULL بماند.

5) Build:
   build-production.cmd

Expected static gates:
   DPS2 Operational Opening v5 Phase 10 verification OK: 34/34 schema-foundation checks passed.
   DPS2 Operational Opening v5 Phase 10B verification OK: 31/31 backend/workflow checks passed.
   DPS2 Operational Opening v5 Phase 10D verification OK: 25/25 UI/integration checks passed.

6) Runtime Smoke Test:
   CIF Party Search
      -> Product/Opening Request
      -> Funding Plan with ACTIVE source-account ownership verification
      -> Create Gate evidence + Runtime Preflight
      -> APPROVED
      -> Create Account
      -> PENDING_ACTIVATION / PENDING_READINESS
      -> Settlement
      -> Activation Readiness
      -> READY
      -> Activate
      -> ACTIVE / COMPLETED

براي Target فعلي اين پروژه:
- Phase 10A Foundation: PASS
- Phase 10A Constraint Repair: PASS
- Phase 10B Runtime Alignment: PASS
بنابراين Migration ديگري براي Phase 10D لازم نيست؛ Source جديد را Overlay کرده و build-production.cmd را اجرا کنيد.

نکات:
- Activate قبل از READY بايد رد شود.
- Funding قبل از Create Account فقط Plan است و SUCCESS ساختگي ندارد.
- Required Compliance/Inquiry Create-Gate checks بدون Evidence واقعي در UI PASS نمي‌شوند.
- Fee/Tax/Service-Charge Obligation ساختگي توليد نمي‌شود.
- SIAH/Compliance/Restriction Evidence به صورت Reference/Status ثبت مي‌شود؛ Integration خارجي واقعي جعل نشده است.
- Phase 9 Account Closure همچنان ACTIVE -> CLOSED باقي مي‌ماند.

Core Banking Prototype 0.3.84
FIX92 - Oracle -> EA XMI Dark/Light Theme Compatibility

1) این نسخه DDL، Migration یا Seed جدید ندارد.
2) تغییر این نسخه صرفاً در Frontend صفحه استخراج Oracle به Enterprise Architect و Guardهای Build است.
3) bin\stop.cmd
4) build-production.cmd
5) خروجی زیر را کنترل کنید:
   Built version: 0.3.84
6) bin\start.cmd
7) نسخه 0.3.84 را در Header کنترل و در مرورگر Ctrl+F5 اجرا کنید.
8) مسیر #/system/oracle-ea-xmi-export را در تم تیره باز کنید؛ Card، Connection Strip، متن‌ها، Badgeها، پیام‌ها و Preview نباید سطح سفید یا متن کم‌کنتراست داشته باشند.
9) همان صفحه را در تم روشن نیز کنترل کنید؛ ساختار و خوانایی باید بدون تغییر عملکرد حفظ شده باشد.
10) Preview Metadata و Export XMI را کنترل کنید؛ این Fix هیچ تغییری در API یا منطق استخراج Oracle ایجاد نمی‌کند.

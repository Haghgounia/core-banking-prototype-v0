Core Banking Prototype 0.3.87 / FIX95

1) اگر از 0.3.86 ارتقا می‌دهید، برای FIX95 هیچ DDL/Migration جدیدی لازم نیست.
2) bin\stop.cmd را اجرا کنید.
3) Source/Patch نسخه 0.3.87 را روی نسخه قبلی قرار دهید.
4) build-production.cmd را اجرا کنید.
5) در خروجی Build کنترل کنید:
   Built version: 0.3.87
6) bin\start.cmd را اجرا کنید.
7) Header باید نسخه 0.3.87 را نشان دهد؛ سپس Ctrl+F5 بزنید.

کنترل تاریخ شمسی استثناء
- مسیر: اطلاعات پایه > تقویم دو > استثناءهای تک‌روز
- تاریخ در Editor با Date Picker شمسی وارد می‌شود.
- ستون «تاریخ استثناء» در Grid باید مانند 1405/12/10 نمایش داده شود.
- مقدار Backend/Oracle بدون تغییر به صورت ISO/Gregorian Canonical ذخیره می‌شود.

کنترل Clock-style Time Picker
- مسیر: اطلاعات پایه > تقویم دو > الگوی هفتگی ساعات کاری یا استثناءهای تک‌روز
- روی آیکون ساعت کلیک کنید.
- پنجره Clock/Dial باز می‌شود؛ ابتدا ساعت و سپس دقیقه را انتخاب کنید.
- نمایش 24 ساعته است و دقیقه‌ها به صورت پیش‌فرض با گام 5 دقیقه هستند.
- مقدار ذخیره‌شده در API/Oracle همچنان مانند 07:30 است.

# Release Notes 0.5.2

Build-only hotfix for DPS2 Four Deposits Phase 5.

- Fixes Java compiler effectively-final lambda capture error in `DepositOpeningAuditService`.
- Adds `verify-dps2-build-hotfix-052.mjs` to Windows and Unix production builds.
- No functional changes.
- No Oracle DDL changes.

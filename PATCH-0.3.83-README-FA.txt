Patch 0.3.83 - FIX91 - تفکیک واژگان و فیلترهای EDU

این Patch را روی نسخه 0.3.82 Extract/Replace کنید.

سپس در Windows:
  bin\stop.cmd
  build-production.cmd

پس از مشاهده:
  Built version: 0.3.83

اجرا کنید:
  bin\start.cmd

در مرورگر Ctrl+F5 بزنید و بالای صفحه نسخه 0.3.83 را کنترل کنید.

اصلاحات:
1) در #/reference-data/general سه گزینه زیر در گروه مستقل «واژگان و اصطلاحات» قرار دارند:
   - واژه‌نامه رومن‌نویسی نام‌ها
   - واژه‌نامه پیشوند و پسوند نام
   - تبدیل نام فارسی به انگلیسی
2) در شش فرم جدید EDU بخش «فیلترهای تخصصی» نمایش داده می‌شود.
3) فرم «مقاطع معتبر هر رشته» رشته و مقطع را با عنوان Lookup نمایش می‌دهد، نه ID خام.
4) فرم «رشته‌ها و گرایش‌ها» فیلتر رشته/گروه والد نیز دارد.


نکته مهم Runtime:
در نسخه 0.3.82 Release Metadata کاملاً همگام نبود و Build می‌توانست قبل از تولید JAR جدید متوقف شود.
در 0.3.83 VERSION، Maven، Frontend و System Specification همگام شده‌اند.
همچنین Source Package دیگر app\BUILD-VERSION را overwrite نمی‌کند؛ بنابراین بدون Build موفق، start.cmd اجازه اجرای JAR قدیمی به‌عنوان نسخه جدید را نخواهد داد.

نیاز به DDL یا تغییر دیتابیس ندارد.
